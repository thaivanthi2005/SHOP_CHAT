include ../mixins/subMenu

header.header
    .header-inner
        .header-logo
            a(href="/")
                img(src=settings.logo, alt=settings.websiteName)
        .header-search
            form(action="/search" method="GET")
                .search-wrap
                    input(
                        type="text"
                        placeholder="Nhập từ khóa..."
                        class="search-input"
                        name="keyword"
                        aria-label="Tìm kiếm"
                    )
                    button(type="submit" class="search-btn") Tìm
        .header-nav
            .inner-menu 
                ul 
                    li
                        a(href="/") TRANG CHỦ
                    li.sub-menu
                        - const pathProduct = "/products"
                        a(href=pathProduct) SẢN PHẨM
                            +subMenu(layoutcategory)
                    li
                        a.cart-link(href="/cart")
                            i.fas.fa-shopping-cart(aria-hidden="true")
                            span GIỎ HÀNG
                            span.cart-badge #{total_quantity}
                    if(user)
                        li
                            a.header-link-user(href='/user/info') #{user.fullName}
                        li
                            a(href='/user/logout') ĐĂNG XUẤT
                        li 
                            a(href='/users/friends') BẠN BÈ
                        li 
                            a(href='/rooms-chat') PHÒNG CHAT
                    else
                        li
                            a.header-link-login(href='/user/login') ĐĂNG NHẬP
                        li
                            a.header-link-register(href='/user/register') ĐĂNG KÝ
