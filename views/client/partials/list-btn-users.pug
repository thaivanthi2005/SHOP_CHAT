.users-nav
  a.btn.btn-primary(href="/users/not-friend") DANH SÁCH NGƯỜI DÙNG
  a.btn.btn-secondary(href="/users/friends") DANH SÁCH BẠN BÈ
  a.btn.btn-info(href="/users/requests") LỜI MỜI ĐÃ GỬI
  a.btn.btn-danger(href="/users/accept")
    | LỜI MỜI KẾT BẠN
    if(user.acceptFriends)
      span.badge.badge-light(badge-users-accept=user.id) #{user.acceptFriends.length}
